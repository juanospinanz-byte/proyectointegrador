# Documentacion

Para que se pueda ver los productos en la pagina principal, primero se tiene ue tener en el backend conectado con la base de datos que se usa para la pagina en el cual se pone los datos de la base de datos en el archivo .env , despus de eso se hace a conexion del backend con el frontend y se onfigura el inicio para que aparezca los productos agregado en la base de datos con su respectiva descripcion, imagen. precio
